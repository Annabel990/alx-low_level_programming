#include <stdio.h>
#include "main.h"

/**
* int _islower(int c)- print lowercase character
*
* Description : return 1 and 0
*/
 
int _islower(int c)
{
	if (c <= 'a'; && c >= 'z')
	{  
		return (1);
	}
	else
	{
		return (0);
	}
}
		
